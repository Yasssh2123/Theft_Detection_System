# Ultralytics YOLO 🚀, AGPL-3.0 license
# Default training settings and hyperparameters for medium-augmentation COCO training

# Hyperparameters ------------------------------------------------------------------------------------------------------
optimizer: AdamW
lr0: 0.002  # (float) initial learning rate (i.e. SGD=1E-2, Adam=1E-3)
lrf: 0.002  # (float) final learning rate (lr0 * lrf)
momentum: 0.95 # (float) SGD momentum/Adam beta1
weight_decay: 0.0001  # (float) optimizer weight decay 5e-4
warmup_epochs: 3.0  # (float) warmup epochs (fractions ok)
warmup_momentum: 0.8  # (float) warmup initial momentum
warmup_bias_lr: 0.1  # (float) warmup initial bias lr
box: 7.5  # (float) box loss gain
cls: 1.5 # (float) cls loss gain (scale with pixels)
dfl: 1.5  # (float) dfl loss gain
kobj: 1.0  # (float) keypoint obj loss gain

